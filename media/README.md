# Media

Add demo and publishing assets here.

Expected files:

- `bilibili_cover.psd` or `bilibili_cover.png`
- `video_script.md`
- `demo_short.mp4`

Large video and PSD files are ignored by the root `.gitignore` by default. If you want to version them in git, adjust `.gitignore` or use Git LFS.

